# 类与对象(上)

## 1.面向过程和面向对象初步认识

> **C语言**是**面向过程**的，关注的是**过程**，分析出**求解问题的步骤**，通过函数调用**逐步解决问题**。
> **C++**是基于**面向对象**的，关注的是**对象**，将一件事情拆分成不同的对象，靠**对象之间的交互完成**。

## 2.类的引入

> **C语言中，结构体中只能定义变量，在C++中，结构体内不仅可以定义变量，也可以定义函数。**

```cpp
struct Student
{
	void SetStudentInfo(const char* name, const char* gender, int age)
{
	strcpy(_name, name);
	strcpy(_gender, gender);
	_age = age;
}
void PrintStudentInfo()
{
	cout<<_name<<" "<<_gender<<" "<<_age<<endl;
}
	char _name[20];
	char _gender[3];
	int _age;
};
int main()
{
	Student s;
	s.SetStudentInfo("Peter", "男", 18);
	return 0;
}
```

上面结构体的定义，**在C++中更喜欢用class来代替**

## 3.类的定义

```cpp
class className
{
	// 类体：由成员函数和成员变量组成
    
}; 	// 一定要注意后面的分号
```

> **class**为定义类的**关键字**，**ClassName**为**类的名字**，**{}中**为**类的主体**，**注意**类定义结束时**后面分号**。
> 类中的**元素**称为**类的成员**：类中的**数据**称为**类的属性**或者**成员变量**; 类中的**函数**称为**类的方法**或者**成员函数**。

**类的两种定义方式：**

> 1. 声明和定义**全部放在类体中**，需要注意：成员**函数**如果**在类中定义**，编译器默认将其当成**内联函数**处理

![image-20250224144357703](C:\Users\Aufire\AppData\Roaming\Typora\typora-user-images\image-20250224144357703.png)

> 2.**声明**放在**类里面**，**定义**放在**类外面**

![image-20250224165951714](C:\Users\Aufire\AppData\Roaming\Typora\typora-user-images\image-20250224165951714.png)

![image-20250224170001375](C:\Users\Aufire\AppData\Roaming\Typora\typora-user-images\image-20250224170001375.png)

> 3.**声明**放在**.h文件**中，类的**定义**放在.**cpp文件**中

![image-20250224144432706](C:\Users\Aufire\AppData\Roaming\Typora\typora-user-images\image-20250224144432706.png)

**一般情况下，更期望采用第二种方式。**

## 4.类的访问限定符及封装

### 4.1访问限定符

> **C++实现封装的方式：用类将对象的属性与方法结合在一块，让对象更加完善，**
>
> **通过访问权限选择性的将其接口提供给外部的用户使用。**

![image-20250224144718134](C:\Users\Aufire\AppData\Roaming\Typora\typora-user-images\image-20250224144718134.png)

> **【访问限定符说明】**
>
> 1. **public**修饰的成员在类外**可以直接被访问**
> 2. **protected和private**修饰的成员**在类外不能直接被访问**(此处protected和private是类似的)，**但是**，**在类里**面**可以被直接访问**
>    - 相当于：**房子里面的人不防，防外面的人**
>
> 1. 访问权限作用域从该访问限定符出现的位置**开始直到下一个访问限定符出现时为止（一个类有多个访问限定符）**
> 2. **class**的默认访问权限为**private**，**struct**为**public**(因为struct要兼容C)
>
> **注意：访问限定符只在编译时有用，当数据映射到内存后，没有任何访问限定符上的区别**

> **【面试题】**
>
> **问题：C++中struct和class的区别是什么？**
>
> 
>
> **解答**：**C++需要兼容C语言**，所以C++中struct可以当成结构体去使用。另外C++中struct还可以用来定义类。
> 和class是定义类是一样的，区别是struct的成员默认访问方式是public，class是的成员默认访问方式是
> private。

### 4.2封装

> **【面试题】** 面向对象的三大特性：**封装、继承、多态**。

**封装：将数据和操作数据的方法进行有机结合，隐藏对象的属性和实现细节，仅对外公开接口来和对象进行交互。**

我们使用类数据和方法都封装到一下。不想给别人看到的，我们使用protected/private把成员**封装**起来。**开放**一些共有的成员函数对成员合理的访问。所以封装**本质**是一种管理。

## 5.类的作用域

> **类定义了一个新的作用域**，类的所有成员都在类的作用域中。**在类体外定义**成员，需要使用 **:: 作用域解析符**
> **指明**成员**属于哪个类域**。

```cpp
class Person
{
	public:
	void PrintPersonInfo();
	private:
	char _name[20];
    char _gender[3];
	int _age;
};
	
void Person::PrintPersonInfo() // 这里需要指定PrintPersonInfo是属于Person这个类域
{
	cout<<_name<<" "_gender<<" "<<_age<<endl;
}
```

**类里面不受作用域限制，类的作用域限制的是外面的访问**

## 6.类的实例化

> **用类类型创建对象的过程，称为类的实例化**

1. **类**只是一个**模型**一样的东西，限定了类有哪些成员，定义出一个类**并没有分配实际的内存空间**空间来存储它
2. 一个类可以实例化出多个对象，**实例化出的对象占用实际的物理空间，存储类成员变量**
3. 做个比方。**类实例化出对象就像现实中使用建筑设计图建造出房子，类就像是设计图**，只设计出需要什
    么东西，但是并没有实体的建筑存在，同样类也只是一个设计，实例化出的对象才能实际存储数据，占
    用物理空间

![image-20250224151701898](C:\Users\Aufire\AppData\Roaming\Typora\typora-user-images\image-20250224151701898.png)

```cpp
#include<iostream>
using namespace std;
class Date
{
public: //共有，可见
	//成员函数
	void Init(int year, int month, int day)
	{
		_year = year;
		_month = month;
		_day = day;
	}
	void Print()
	{
		cout << _year << "-" << _month << "-" << _day << endl;
	}

private: //私有，不可见
	//成员变量
	int _year;
	int _month;
	int _day;
};
int main()
{
	Date d1;//d1 是类实例化出的 对象

	//访问对象中的成员函数
	d1.Init(2025, 2, 25);
	d1.Print();

	return 0;
} 
```

**类实例化出对象，相当于定义出了类的成员变量**

> **实例化就是用自己定义的类型定义出对象**
>
> - 内置类型，基本类型：int/char/double
> - 自定义类型：class/struct

## 7.类对象模型

### 7.1 如何计算类对象的大小

![image-20250224171353141](C:\Users\Aufire\AppData\Roaming\Typora\typora-user-images\image-20250224171353141.png)**s1是实例化出的对象**

**只计算成员变量**，不计算成员函数

### 7.2 类对象的存储方式猜测

- **只保存成员变量，成员函数存放在公共的代码段**

![image-20250224173945992](C:\Users\Aufire\AppData\Roaming\Typora\typora-user-images\image-20250224173945992.png)

> **一个类**可以实例化出**N个对象**，**每个对象的成员变变量**都可以**存储不同的值**，但是**调用的函数**是**同一个**
>
> - 如果每个对象都放成员函数，而这些成员函数却是一样的，会浪费空间

结论：**一个类的大小，实际就是该类中”成员变量”之和（定义的对象的大小）**，当然也要进行内存对齐。

- 内存对其规则

注意：注意**空类的大小**，空类比较特殊，编译器给了**空类一个字节**来**唯一标识这个类**。

- 为什么是1，而不是0。开一个字节不是为了存数据，而是占位（表示对象存在）。

### 7.3 结构体内存对齐规则

> 1. 第一个成员在与结构体偏移量为0的地址处。
> 2. 其他成员变量要对齐到某个数字（对齐数）的整数倍的地址处。
>
> - 注意：对齐数 = 编译器默认的一个对齐数 与 该成员大小的较小值。VS中默认的对齐数为8
> - 每个成员变量都有一个对齐数，这个对齐数 = min（自己的大小，默认对齐数）
>
> 3. 结构体总大小为：最大对齐数（所有变量类型最大者与默认对齐参数取最小）的整数倍。
> 4. 如果嵌套了结构体的情况，嵌套的结构体对齐到自己的**最大对齐数的整数倍**处，结构体的整体大小就是
>
> ​	所有最大对齐数（含嵌套结构体的对齐数）的整数倍。

**【面试题】**

1. 结构体怎么对齐？ 为什么要进行内存对齐
2. 如何让结构体按照指定的对齐参数进行对齐
3. 什么是大小端？如何测试某台机器是大端还是小端，有没有遇到过要考虑大小端的场景？

## 8.this指针

### 8.1 this指针的引出

> **以日期类Date为例**

```cpp
class Date
{
public: 
	void Init(int year, int month, int day)
	{
		_year = year;
		_month = month;
		_day = day;
	}
	void Print()
	{
		cout << _year << "-" << _month << "-" << _day << endl;
	}

private:
	int _year;
	int _month;
	int _day;
};
int main()
{
	Date d1;
	d1.Init(2025, 2, 25);
	d1.Print();

	return 0;
}
```

> C++编译器给每个“**非静态的成员函数**“增加了一个**隐藏**的**指针参数**，让该指针指向当前对象(函数运行时调用该函数的对象)，在函数体中所有成员变量的操作，都是通过该指针去访问。只不过**所有的操作**对用户是**透明的**，即用户不需要来传递，**编译器自动完成**。

 ![image-20250225181357637](C:\Users\Aufire\AppData\Roaming\Typora\typora-user-images\image-20250225181357637.png)

我们在**实际操作中**会看到**Print函数是没有传参**的，但是**实际上**，**参数是一个隐藏的指针**，，只不过是编译器自动完成，不要需要用户来操作，也就对用户来说是透明的，在**调用函数的时候**，**实际上**也是会有传参的(**传地址**)

- **Date* this一定是放在最前面，相当于多了一个参数**

- **this是谁调用这个成员函数，this就指向谁**

### 8.2 this指针的特性

> 1. this指针的类型：类类型* const
>
> 2. 只能在“**成员函数**”的**内部**使用
>
> 3. this指针**本质上**其实是**一个**成员函数的**形参**，是对象调用成员函数时，**将对象地址作为实参**传递给this形参。所以**对象中不存储this指针**。
>
> 4. this指针是成员函数**第一个**隐含的指针形参，一般情况由编译器通过ecx寄存器自动传递，不需要用户传递

**【面试题】**

> 1. **this指针存在哪里？**（也就是存在进程地址空间的哪个区域？）
>
>    - **存在栈上的**，因为他是一个**形参**。（vs下是在ecx这个寄存器，来传递）
>    - 寄存器有很多，每个寄存器都有自己的作用（常见：eax、ebx、ecx）
>    - 寄存器最大的特点是快，比内存快，是电脑里面最快的存储设备，但是材料贵，存储容量也小
>    - 所以很多小数据，如果经常用，就可以放寄存器，可以提高效率
>
>    ![image-20250225205139048](C:\Users\Aufire\AppData\Roaming\Typora\typora-user-images\image-20250225205139048.png)
>
> 2. **this指针可以为空吗？**

```cpp
// 1.下面程序能编译通过吗？
// 2.下面程序会崩溃吗？在哪里崩溃
class A
{
public:
	void PrintA()
	{
		cout<<_a<<endl;
	}
	void Show()
	{
		cout<<"Show()"<<endl;
	}
private:
	int _a;
};
int main()
{
	A* p = nullptr; //p->Print(p); //对象调就传对象的地址，指针调就传指针
	p->PrintA(); //崩溃
	p->Show();   //正常运行
}
```

- **成员函数Show存在公共的代码段，所以p->Show()这里不会去p指向的对象上找**
- **访问成员函数才回去找**
- **对象调就传对象的地址，指针调就传指针**



# 类与对象（中）

## 1.类的6个默认成员函数

> **如果一个类中什么成员都没有，简称为空类。空类中什么都没有吗？并不是的，任何一个类在我们不写的情
> 况下，都会自动生成下面6个默认成员函数**

![image-20250225210256522](C:\Users\Aufire\AppData\Roaming\Typora\typora-user-images\image-20250225210256522.png)

> 我们不实现时，编译器生成拷贝构造和operator=，会完成按字节的值拷贝(**浅拷贝**)
>
> **也就是说，有些类，我们是不需要去实现拷贝构造和operator=的，因为编译器默认生成就可以用，比如Date(日期类)就是这样**

```cpp
d1 == d2
```

我们不写，编译器会自动生成吗？不会，**因为它不是默认成员函数** 



 

## 2.构造函数

### 2.1 概念

> **构造函数**是一个**特殊的成员函数**，**名字与类名相同**,**创建类类型对象时由编译器自动调用**，保证**每个数据成员**
> **都有 一个合适的初始值（任务是完成初始化）**，并且**在对象的生命周期内只调用一次**。

### 2.2 特性

> **构造函数**是特殊的成员函数，需要注意的是，构造函数的虽然名称叫构造，但是需要注意的是构造函数的主
> 要任务**并不是**开空间**创建对象**，**而是初始化对象**。

其特征如下：

> 1. **函数名与类名相同**。
> 2. **无返回值**。
> 3. 对象实例化时编译器**自动调用**对应的构造函数。
> 4. 构造函数**可以重载**。

```cpp
class Date
{
public :
    //函数重载
	// 1.无参构造函数
	Date ()
	{}
	// 2.带参构造函数
	Date (int year, int month , int day )
	{
		_year = year ;
		_month = month ;
		_day = day ;
	}
private :
	int _year ;
	int _month ;
	int _day ;
};
void TestDate()
{
	Date d1; // 调用无参构造函数，调用无参的不用带括号，有参才用
    //对象实例化（对象构造）时自动调用
	Date d2 (2015, 1, 1); // 调用带参的构造函数
    
    //以下代码的函数：声明了d3函数，该函数无参，返回一个日期类型的对象
	Date d3();
}
```

**注意**：如果通过**无参**构造函数创建对象时，对象**后面不用跟括号**，**否则就成了函数声明**

> 5. 如果类中**没有显式定义构造函数**，则C++**编译器**会**自动生成**一个**“无参”**的**默认构造函数**，一旦**用户显式定**
>    **义编译器将不再生成。**
>
> ```cpp
> class Date
> {
> public:
>     /*
>     // 如果用户显式定义了构造函数，编译器将不再生成
>     Date (int year, int month, int day)
>     {
>     _year = year;
>     _month = month;
>     _day = day;
>     }
>     */
> private:
>     int _year;
>     int _month;
>     int _day;
> };
> void Test()
> {
>     // 没有定义构造函数，对象也可以创建成功，因此此处调用的是编译器生成的默认构造函数
> 	Date d;
> }
> ```
>
> - ```cpp
>   //更好的方式 - >全缺省
>   Date (int year = 1900, int month = 1, int day = 1)
>       {
>       _year = year;
>       _month = month;
>       _day = day;
>       }
>   ```
>
>   **如果传参了，就用传的数据，如果没有传参，就用缺省数据**
>
> 6. **无参**的构造函数和**全缺省**的构造函数**都称为默认构造函数（不用传参都能调用）**，并且**默认构造函数只能有一个**。
>
>    **注意：**无参构造函数、全缺省构造函数、是我们没写时编译器默认生成的构造函数，都可以认为是**默认成员函数（我们不写，编译器都会自动生成）**。
>
>    **特点：**都不用传参数
>
>    ```cpp
>    // 默认构造函数
>    class Date
>    {
>    public:
>        Date()
>        {
>        _year = 1900 ;
>        _month = 1 ;
>        _day = 1;
>        }
>        Date (int year = 1900, int month = 1, int day = 1)
>        {
>        _year = year;
>        _month = month;
>        _day = day;
>        }
>    private :
>        int _year ;
>        int _month ;
>        int _day ;
>    };
>    // 以下测试函数能通过编译吗？不能
>    void Test()
>    {
>    	Date d1;//有多个默认构造函数，不知道选哪个
>    }
>    ```
>
> 7. 关于编译器生成的默认成员函数，很多童鞋会有疑惑：在我们不实现构造函数的情况下，编译器会生成
>    默认的构造函数。但是看起来默认构造函数又没什么用？d对象调用了编译器生成的默认构造函数，但
>    是d对象year/month/_day，依旧是随机值。也就说在这里**编译器生成的默认构造函数并没有什么卵用？？**_
>
> 
>
>   解答：C++把类型分成内置类型(基本类型)和自定义类型。**内置类型**就是**语法已经定义好的类型**：如
>   int/char...，**自定义类型**就是我们**使用class/struct/union自己定义的类型**，看看下面的程序，就会发现
>   **编译器生成默认的构造函数会对自定类型成员**_t**调用的它的默认成员函数**，而**内置类型不会处理**
>
>   ```cpp
> class Time
> {
> public:
>     Time()
>     {
>         cout << "Time()" << endl;
>         _hour = 0;
>         _minute = 0;
>         _second = 0;
>     }
> private:
>     int _hour;
>     int _minute;
>     int _second;
> };
> class Date
> {
>    private:
>     // 基本类型(内置类型)
>     int _year;
>     int _month;
>     int _day;
>     // 自定义类型
>     Time _t;
> };
> int main()
> {
>     Date d;
>     return 0;
> }
>   ```
>
> 
>
> 8. 成员变量的命名风格
>
> ```cpp
> // 我们看看这个函数，是不是很僵硬？
> class Date
> {
> public:
>     Date(int year)
>     {
>     // 这里的year到底是成员变量，还是函数形参？
>     year = year;
>     }
> private:
> 	int year;
> };
> // 所以我们一般都建议这样
> class Date
> {
>     public:
>     Date(int year)
>     {
>     _year = year;
>     }
> private:
> 	int _year;
> };
> // 或者这样。
> class Date
> {
> public:
>     Date(int year)
>     {
>     m_year = year;
>     }
> private:
> 	int m_year;
> };
> // 其他方式也可以的，主要看公司要求。一般都是加个前缀或者后缀标识区分就行。
> ```
>
> 以上这些**前缀或者后缀**都是为了更好的**区分形参和成员变量**

## 3.析构函数

### 3.1概念

> 前面通过构造函数的学习，我们知道一个对象时怎么来的，那一个对象又是怎么没呢的？
>
> 析构函数：与构造函数功能相反，析构函数不是完成对象的销毁，局部对象销毁工作是由编译器完成的。而
> **对象在销毁时会自动调用（析构函数，完成类的一些资源清理工作）。**

### 3.2特征

> 析构函数是**特殊的成员函数**。
>
> 其**特征**如下：
>
> 1. 析构函数名是在**类名前**加上字符 **~**。
>
> 2. **无参数无返回值**。
>
> 3. **一个类**有且**只有一个析构函数**。若**未显式定义**，系统会**自动生成**默认的析构函数。
>
> 4. 对象**生命周期结束**时，C++编译系统系统**自动调用析构函数**。
>
>    ```cpp
>    typedef int DataType;
>    class SeqList
>    {
>    public :
>        SeqList (int capacity = 10)
>        {
>            _pData = (DataType*)malloc(capacity * sizeof(DataType));
>            assert(_pData);
>            _size = 0;
>            _capacity = capacity;
>        }
>        ~SeqList()
>        {
>            if (_pData)
>            {
>                free(_pData ); // 释放堆上的空间
>                _pData = NULL; // 将指针置为空
>                _capacity = 0;
>                _size = 0;
>            }
>        }
>    private :
>        int* _pData ;
>        size_t _size;
>        size_t _capacity;
>    };
>    ```
>
> 5. 关于编译器自动生成的析构函数，是否会完成一些事情呢？
>
>   下面的程序我们会看到，编译器生成的默认析构函数，对会**自定义类型**成员**调用**它的**析构函数**。
>
>   和构造函数一样，**对内置类型不作处理**，对自定义类型会自动调用
>
> ```cpp
> class String
> {
> public:
>     String(const char* str = "jack")
>     {
>         _str = (char*)malloc(strlen(str) + 1);
>         strcpy(_str, str);
>     }
>     ~String()
>     {
>         cout << "~String()" << endl;
>         free(_str);
>     }
> private:
> 	char* _str;
> };
> class Person
> {
> private:
> 	String _name;
> 	int _age;
> };
> int main()
> {
> 	Person p;
> 	return 0;
> }
> ```
>
> 

## 4.拷贝构造函数

### 4.1概念

> **构造函数**：**只有单个形参**，该形参是对本类**类型对象的引用**(**一般常用const修饰**)，在**用**已存在的类**类型对象**
> **创建新对象时由编译器自动调用**

### 4.2特征

> 拷贝构造函数也是特殊的成员函数
>
> 其**特征**如下：
>
> 1. 拷贝构造函数是**构造函数的一个重载形式**。
> 2. 拷贝构造函数的**参数只有一个**且必须使用**引用传参**，否则，**传值方式会引发无穷递归调用（调用一次拷贝构造的时间复杂度是O(1)）**。
>
> ```cpp
> class Date
> {
> public:
> 	Date(int year = 1900, int month = 1, int day = 1)
> 	{
> 		_year = year;
> 		_month = month;
> 		_day = day;
> 	}
> 	Date(const Date& d)
>     //引用，d是d1的别名，等价于Date& d = d1;此处就没有拷贝构造了
>     //加const，避免了d._day = _day的错误，保证了“我用你的值(d._day)，但是我不修改你的值”
> 	{
> 		_year = d._year;
> 		_month = d._month;
> 		_day = d._day;
> 	}
>     void Print()
> 	{
> 		cout << _year << "-" << _month << "-" << _day << endl;
> 	}
> private:
> 	int _year;
> 	int _month;
> 	int _day;
> };
> void func(Date& d)//不加&就要先拷贝构造，加了&就不用调用，直接进入函数
> {
> 
> }
> int main()
> {
> 	Date d1;
> 
>     //这两个写法都是调用拷贝构造
> 	Date d2(d1);//调用的拷贝构造
>     Date d3 = d1;
>     d1.Print();
> 	d2.Print();
> 	d3.Print();
> 
>     func(d1);
> 	return 0;
> }
> ```

**调用的就是拷贝构造，但是在调用函数时要先传参数，传参的过程又是一个拷贝构造的调用**

![image-20250226165555197](C:\Users\Aufire\AppData\Roaming\Typora\typora-user-images\image-20250226165555197.png)

**对象初始化时（实参传形参时的赋值初始化）自动调用构造函数（拷贝构造）**

> 3.**若未显示定义，系统生成默认的拷贝构造函数。** 
>
> **默认**的拷贝构造函数对象**按内存存储**，**按字节序**完成拷贝，这种拷贝我们叫做**浅拷贝**，或者**值拷贝**。
>
> ```cpp
> class Date
> {
> public:
> 	Date(int year = 1900, int month = 1, int day = 1)
> 	{
> 		_year = year;
> 		_month = month;
> 		_day = day;
> 	} 
> private:
> 	int _year;
> 	int _month;
> 	int _day;
> };
> int main()
> {
> 	Date d1;
> 	// 这里d2调用的默认拷贝构造完成拷贝，d2和d1的值也是一样的。
> 	Date d2(d1);
> 	return 0;
> }
> ```
>
> 4.那么**编译器生成的默认拷贝构造函数已经可以完成字节序的值拷贝了**，我们还需要自己实现吗？当然像
> 日期类这样的类是没必要的。那么下面的类呢？验证一下试试？
>
> ```cpp
> // 这里会发现下面的程序会崩溃掉？这里就需要我们以后讲的深拷贝去解决。
> class String
> {
> public:
> 	String(const char* str = "jack")
> 	{
> 		_str = (char*)malloc(strlen(str) + 1);
> 		strcpy(_str, str);
> 	}
> 	~String()
> 	{
> 		cout << "~String()" << endl;
> 		free(_str);
> 	}
> private:
> 	char* _str;
> };
> int main()
> {
> 	String s1("hello");
> 	String s2(s1);
> }
> ```

**对于栈:**

![ ](C:\Users\Aufire\AppData\Roaming\Typora\typora-user-images\image-20250302125106664.png)

为了避免内存被释放两次，我们**要自己去实现深拷贝的拷贝构造和operator**

## 5.赋值操作符重载

### 5.1 运算符重载

> **C++为了增强代码的可读性引入了运算符重载，运算符重载是具有特殊函数名的函数**，也具有其返回值类
> 型，函数名字以及参数列表，其返回值类型与参数列表与普通的函数类似。
>
> **运算符的重载是为了让自定义类型可以像内置类型一样去使用运算符**
>
> 函数名字为：关键字**operator后面接需要重载的运算符符号**。
>
> 函数原型：**返回值类型 operator操作符(参数列表)**
>
> **注意：**
>
> - 不能通过连接其他符号来创建新的操作符：比如operator@
> - 重载操作符**必须有一个类类型或者枚举类型的操作数**
>
> - 用于**内置类型**的操作符，其**含义不能改变**，例如：内置的整型+，不能改变其含义(即整形不能进行重载,**自定义类型才能**)
> - 作为类成员的重载函数时，其形参看起来比操作数数目少1，成员函数的操作符有一个默认的形参**this，限定为第一个形参**
> - **.*(点星,访问后解引用)** 、**::** 、**sizeof** 、**?:(三目运算符)** 、**.** 注意以上5个运算符不能重载。这个经常在笔试选择题中出现。

```cpp
// 全局的operator==
class Date
{
public:
	Date(int year = 1900, int month = 1, int day = 1)
	{
		_year = year;
		_month = month;
		_day = day;
	}
	//private:
	int _year;
	int _month;
	int _day;
};
// 这里会发现运算符重载成全局的就需要成员变量是共有的，那么问题来了，封装性如何保证？
// 这里其实可以用我们后面学习的友元解决，或者干脆重载成成员函数。
bool operator==(const Date& d1, const Date& d2)
{
	//先比年，如果年相同，就比月，如果还相同，再比天
	return d1._year == d2._year
		&& d1._month == d2._month
		&& d1._day == d2._day;
}
int main()
{
	Date d1(2018, 9, 26);
	Date d2(2018, 9, 27);
	d1 == d2;
	//上行代码编译器会转换成->operator==(d1,d2) //但是一般不会这样写，因为这样写可读性不好

	cout << (d1 == d2) << endl;
	return 0;
}
```

**上面代码**编译会出错![image-20250226183424033](C:\Users\Aufire\AppData\Roaming\Typora\typora-user-images\image-20250226183424033.png)

**作为类成员的重载函数时，其形参看起来比操作数数目少1，成员函数的操作符有一个默认的形参this，限定为第一个形参**
**相等:**

```cpp
class Date
{
public:
	Date(int year = 1900, int month = 1, int day = 1)
	{
		_year = year;
		_month = month;
		_day = day;
	}
	// bool operator==(Date* this, const Date& d2)
	// 这里需要注意的是，左操作数是this指向的调用函数的对象
	bool operator==(const Date& d2)
        //调本质上是->d1.operator==(&d1,d2)，&表示的是取地址给形参的this指针，不是引用
	{
		return _year == d2._year //本质	上是this -> _year == d2._year
			&& _month == d2._month
			&& _day == d2._day;
	}
private:
	int _year;
	int _month;
	int _day;
};
void Test()
{
	Date d1(2018, 9, 26);
	Date d2(2018, 9, 27);
    //调用
	cout << (d1 == d2) << endl;//0
    //或者
    d1.operator==(d2)//但实际调用时，因为this指针是不允许显示传的，所以&d1不可见
}
```

**大于:**

```cpp
class Date
{
public:
	Date(int year = 1900, int month = 1, int day = 1)
	{
		_year = year;
		_month = month;
		_day = day;
	}
	// bool operator==(Date* this, const Date& d2)
	// 这里需要注意的是，左操作数是this指向的调用函数的对象
	bool operator>(const Date& d2)
	{
		if (_year > d2._year)
			return true;
		else if (_year == d2._year && _month > d2._month)
			return true;
		else if (_year == d2._year && _month == d2._month && _day > d2._day)
			return true;
		return false;
	}
private:
	int _year;
	int _month;
	int _day;
};
int main()
{
	Date d1(2018, 9, 27);
	Date d2(2018, 9, 26);
	cout << (d1 > d2) << endl;//1
	return 0;
}
```

### 5.2 赋值运算符重载

```cpp
#include<iostream>
using namespace std;
class Date
{
public:
	Date(int year = 1900, int month = 1, int day = 1)//构造函数，用于初始化
	{
		_year = year;
		_month = month;
		_day = day;
	}
	Date(const Date& d) //拷贝构造
	{
		_year = d._year;
		_month = d._month;
		_day = d._day;
	}
	Date& operator=(const Date& d)//这里如果不加引用的话，就会先调用上面的拷贝构造，然后再进行这里，避免重复的调									用，可以加上引用，改成别名，就不会再调用上面的拷贝构造了，只在一开始调用拷贝构造
	{
		if (this != &d)//赋值前后两个数不相等，防止自己给自己赋值
		{
			_year = d._year;
			_month = d._month;
			_day = d._day;
		}
	}
private:
	int _year;
	int _month;
	int _day;
};
int main()
{
	d1 = d2;
}
```

赋值运算符主要有四点：

1. 参数类型
2. 返回值
3. 检测是否自己给自己赋值(不能自己给自己赋值)
4. 返回*this
5. 一个类**如果没有显式定义**赋值运算符重载，**编译器**也**会自动生成**一个，完成对象按字节序的值拷贝。

**自定义类型*传参数*和*返回值*时，在可以的情况下，尽量使用*引用*，减少拷贝**

**连续赋值(i = j = k)运用赋值运算符的重载(operator=)的时候，返回值和传参都尽量用引用(Date &d)，记得返回return *this（返回自己）**

![image-20250302200117389](C:\Users\Aufire\AppData\Roaming\Typora\typora-user-images\image-20250302200117389.png)

```cpp
Date d3(d1)//d3还不存在，构造d3时用d1去初始化
Date d4 = d1 //特别注意：这里是拷贝构造，不是operator=,因为d4还不存在，得先构造，构造时用d1去初始化d4
两个写法是等价的，相当于编译器都会识别成调用拷贝构造
```

**已经定义的就赋值，没有定义的就构造**

## 6.日期类的实现

### 日期类的大小比较

```cpp
#include<iostream>
using namespace std;
//实现一个完善的日期类
class Date
{
public:
	int GetMonthDay(int year, int month)
	{
		int MonthDay[13] = { 0,31,28,31,30,31,30,31,31,30,31,30,31 };//第一个元素是0，空一位，保证每一个月对应它的天数
		if (month == 2 && (year % 4 == 0 && year % 100 != 0) || (year % 400 == 0))
		{
			return 29;//如果是2月，且是润年就直接返回29天
		}
		return MonthDay[month];;
	}
	Date(int year = 0, int month = 1, int day = 1)
	{
		if (year >= 0 //至少是公元年
			&& month >= 1 && month <= 12
			&& day >= 1 && day <= GetMonthDay(year,month))
		{
			_year = year;
			_month = month;
			_day = day;
		}
		else
		{
			cout << "非法日期" << endl;
		}
	}
	void Print()
	{
		cout << _year << "-" << _month << "-" << _day << endl;
	}
	bool operator==(const Date& d)
	{
		return (_year == d._year && _month == d._month && _day == d._day);
	}
	bool operator>(const Date& d)
	{
		if (_year > d._year)
		{
			return true;
		}
		else if (_year == d._year && _month > d._month)
		{
			return true;
		}
		else if (_year == d._year && _month == d._month && _day > d._day)
		{
			return true;
		}
		return false;
	}
	bool operator<(const Date& d)
	{
		if (_year < d._year)
		{
			return true;
		}
		else if (_year == d._year && _month < d._month)
		{
			return true;
		}
		else if (_year == d._year && _month == d._month && _day < d._day)
		{
			return true;
		}
		return false;
	}
	bool operator>=(const Date& d)
	{
		if (_year > d._year)
		{
			return true;
		}
		else if (_year == d._year && _month > d._month)
		{
			return true;
		}
		else if (_year == d._year && _month == d._month && _day >= d._day)
		{
			return true;
		}
		return false;
	}
	bool operator<=(const Date& d)
	{
		if (_year < d._year)
		{
			return true;
		}
		else if (_year == d._year && _month < d._month)
		{
			return true;
		}
		else if (_year == d._year && _month == d._month && _day <= d._day)
		{
			return true;
		}
		return false;
	}
private:
	int _year;
	int _month;
	int _day;
};
int main()
{
	Date d1;
	d1.Print();

	Date d2(2025,2,27);
	d2.Print();

	Date d3(2025, 2, 29);//平年2月没有29日
	d3.Print();

	Date d4(2025, 4, 31);//四月没有31日
	d4.Print();
	Date d5(2025, 3, 5);
	d5.Print();
	cout << (d2 == d5) << endl;
	cout << (d2 < d5) << endl;
	cout << (d2 <= d5) << endl;
	return 0;
}
```

如果要加入**小时、分、秒**等精确数据，用operator进行比较的时候需要每一个函数都进行修改，再加上代码重复性高，很麻烦

```cpp
	int _hour;
	int _minuet;
	int _second;
```

**优化代码：**

```cpp
	bool operator==(const Date& d)
	{
		return (_year == d._year && _month == d._month && _day == d._day);
	}
	bool operator<(const Date& d)
	{
		if (_year < d._year)
		{
			return true;
		}
		else if (_year == d._year && _month < d._month)
		{
			return true;
		}
		else if (_year == d._year && _month == d._month && _day < d._day)
		{
			return true;
		}
		return false;
	}
    bool operator<=(const Date& d)//复用上面的来实现
    {
        return *this < d || *this == d;
    }
    bool operator>(const Date& d)
    {
        return !(*this <= d); //!是逻辑反的意思
    }
    bool operator>=(const Date& d)
    {
        return !(*this < d);
    }
    bool operator!=(const Date& d)
    {
        return !(*this == d);
    }
```

> 以上代码，仅需实现一个<=，剩下的就能利用逻辑反来实现，
>
> 如果需要加上时、分、秒，只需在<和==加上时、分、秒，剩下的不用变
>
> **复合是内聚高，耦合度是类与类之间的，内聚越高越好，耦合度越低越好，代码越简单，耦合度越低，越容易掌控**
>
> **高内聚（类内部成员之间的关系）、低耦合（类和类之间的关系），以后更多指的是模块间的关系**
>
> - 类比于部门间：**部门内**的凝聚力越高**内聚度越高越好**、战斗力越强，**部门和部门之间**的联系越低越好**耦合度越低越好**，一个部门垮了，不至于连累别的部门

### 日期类的加减

#### 加减天数

>  是否要重载一个运算符，看的是这个运算符是否对这个类的对象有意义
>
>  ```cpp
>  d1 + 10; //d1不变
>  d1 += 10;//d1变了
>  ```
>
>  **单纯的加**
>
>  ```cpp
>  	Date operator + (int day)//加天数，返回的是一个日期
>  	{
>  		Date ret(*this);//用d5拷贝构造一个ret日期类
>  		ret._day += day;//当月天数加上要加的天数
>  		while (ret._day > GetMonthDay(ret._year, ret._month))//如果加后的天数大于该月的最大天数，则月数进一
>  		{
>  			ret._day -= GetMonthDay(ret._year, ret._month);
>  			ret._month++;
>  			if (ret._month == 13)//当月份为13时，年份加一
>  			{
>  				ret._year++;
>  				ret._month = 1;//月份重置为一月
>  			}
>  		}
>  		return ret;//因为ret出了作用域就会被销毁，所以不能用引用返回，会很危险
>  	}
>  ```
>
>  **加等，自己变**
>
>  ```cpp
>  	Date operator +=(int day)//+=要自己也变，直接返回自己就行了，不用拷贝构造
>  	{
>  		_day += day;//当月天数加上要加的天数
>  		while (_day > GetMonthDay(_year, _month))//如果加后的天数大于该月的最大天数，则月数进一
>  		{
>  			_day -= GetMonthDay(_year, _month);
>  			_month++;
>  			if (_month == 13)
>  			{
>  				_year++;
>  				_month = 1;
>  			}
>  		}
>  		return *this;
>  	}
>  ```
>
>  **复用+=来实现+:**
>
>  ```cpp
>  	//复用+=实现
>  	Date operator + (int day)//加天数，返回的是一个日期
>  	{
>  		Date ret(*this); //ret = *this
>  		ret += day;
>  		return ret;
>  	}
>  ```
>
>  **单纯减:**
>
>  ```cpp
>  	Date operator - (int day)//减天数，返回的是一个日期
>  	{
>      if (day < 0)//如果传了个负数，如果减去负数，就会变成加的了
>  		{
>  			return *this += -day;//将传的负数变成正数，就能正常减去了
>  		}
>  		Date ret(*this);//用d5拷贝构造一个ret日期类
>  		ret._day -= day;//当月天数加上要减的天数
>  		while (ret._day <= 0)//如果减后变成了负数，且每一个月都没有0日
>  		{
>  			ret._month--;
>  			if (ret._month == 0)
>  			{
>  				ret._year--;
>  				ret._month = 12;//月份重置为12月
>  			}
>  			ret._day += GetMonthDay(ret._year, ret._month);//则向前一个月借天数
>  		}
>  		return ret;//因为ret出了作用域就会被销毁，所以不能用引用返回，会很危险
>  	}
>  ```
>
>  **为什么要这样：**![image-20250302213352559](C:\Users\Aufire\AppData\Roaming\Typora\typora-user-images\image-20250302213352559.png)
>
>  ![image-20250302213346381](C:\Users\Aufire\AppData\Roaming\Typora\typora-user-images\image-20250302213346381.png)
>
>  **减等:**
>
>  ```cpp
>  Date operator -=(int day)//-=要自己也变，直接返回自己就行了，不用拷贝构造
>  {
>    	if (day < 0)
>  		{
>  			return *this += -day;
>  		}
>  	_day -= day;//当月天数减上要减的天数
>  	while (_day <= 0)//如果减后为负数，且每一个月都没有0日
>  	{
>  		_month--;
>  		if (_month == 0)//先判断是否借到了上一年
>  		{
>  			_year--;
>  			_month = 12;
>  		}
>  		_day += GetMonthDay(_year, _month);
>  
>  	}
>  	return *this;
>  }
>  ```
>
>  **复用-=来实现-:**
>
>  ```cpp
>  	//复用-=实现
>  	Date operator - (int day)//减天数，返回的是一个日期
>  	{
>  		Date ret(*this); //ret = *this
>  		ret -= day;
>  		return ret;
>  	}
>  ```
>
>  **前置加加**
>
>  ```cpp
>  Date& operator++()//这里后面不能加const
>  {
>  	*this += 1;//因为这里会调用+=的运算符重载，+=是可读可写，用只读去调用可读可写，不行！
>  	return *this;//返回加之后的值
>  }
>  ```
>
>  **后置加加**
>
>  ```cpp
>  Date operator++(int)//为了构成函数重载，约定这个是后置加加
>  {
>      Date tmp(*this);//tmp出作用域之后会被销毁，则不能用引用返回，即返回类型是Date，而不是Date&
>      *this += 1;
>      return tmp;//返回加之前的值
>  }
>  ```
>
>  **前置减减**
>
>  ```cpp
>  Date& operator--()
>  {
>      *this -= 1;
>      return *this;//返回减之后的值
>    }
>  ```
>
>  ```cpp
>  int main()
>  {
>  	--d2;
>  	d2.Print();
>  	return 0;
>  } 
>  ```
>
>  **后置减减**
>
>  ```cpp
>  Date operator--(int)//为了构成函数重载，约定这个是后置减减
>  {
>        //拷贝构造一下
>      Date tmp(*this);//tmp出作用域之后会被销毁，则不能用引用返回，即返回类型是Date，而不是Date&
>      *this -= 1;
>      return tmp;//返回减之前的值
>  }
>  ```

**能调用前置就尽量调用前置，后置会多一次拷贝构造**

#### 加减日期类

**日期类相减:**

> 可以用**小的日期往大的日期加**，加了多少次就是差多少天

```cpp
//d1 - d2
int operator-(const Date& d)
{
    int flag = 1;//如果第一个大，第二个小
	Date max = *this;//假设d1 > d2
    Date min = d;
    if(max < min)//如果d1 < d2
    {
		max = d;
        min = *this;
        flag = -1;//如果第一个小，第二个大
    }
    int n = 0;//统计一共加了多少次
    while(min != max)//min一直加，知道min == max 
    {
        ++min;
        ++n;
    }
    //大减小，n是正数
    //小减大，n是负数
    return n*flag;//*flag后可以正常返回正负数
}
```



## 7.const成员函数

### 7.1 const修饰类的成员函数

> 将**const修饰的类成员函数**称之为**const成员函数**，const修饰类成员函数，实际**修饰**该成员函数**隐含的this指针**，表明在该成员函数中**不能对类的任何成员进行修改**。
>
> ![image-20250303165812307](C:\Users\Aufire\AppData\Roaming\Typora\typora-user-images\image-20250303165812307.png)
>
> 因为**this指针是隐含的，我们看不见**，所以要加**const**修饰要在**括号的外面**加上
>
> **请思考下面的几个问题：**
>
> 1. const对象可以调用非const成员函数吗？**不行**
> 2. 非const对象可以调用const成员函数吗？**可以**
> 3. const成员函数内可以调用其它的非const成员函数吗？**不行**
> 4. 非const成员函数内可以调用其它的const成员函数吗？**可以**
>
> 

## 8.取地址及const取地址操作符重载

> **取地址**也是**默认成员函数**，没有operator&也能调用

### **取地址**

```cpp
Date* operator&()//返回类型是指针
{
	return this;//返回this指针
}
```

> 实际上，以上代码是没有意义的，如果说有意义的话，就是**可以**让他**取不到**我的**地址**
>
> ```cpp
> return nullptr;
> ```
>
> 最终返回的地址全部都是0

### const取地址

```cpp
const Date* operator&() const 
    //这里的第一个参数是const Date*类型的，返回第一个参数的地址，所以返回值类型也要是const Date*
{
	return nullptr;
}
```

**对象是const的，取地址，就是const Date* **

> 成员变量最好都加上const，防止调用operator的时候，导致权限的放大

**只要成员函数中不直接(=)或者间接(+=)改变成员变量，建议都加上const**

# 类与对象（下）

## 1.再谈构造函数

### 1.1 构造函数体赋值

```cpp
	Date(int year = 0, int month = 1, int day = 1)
	{
		//函数体内赋值
		_year = year;
		_month = month;
		_day = day;
	}
```

> 虽然上述构造函数调用之后，对象中已经有了一个初始值，但是不能将其称作为类对象成员的初始化，**构造函数体中的语句**只能将其称作为**赋初值**，而不能称作初始化。因为**初始化只能初始化一次**，而**构造函数体内可以多次赋值**。

### 1.2 初始化列表

> 初始化列表：以一个**冒号开始**，接着是一个以**逗号分隔**的**数据成员列表**，每个"**成员变量**"后面跟一个**放在括号中的初始值或表达式**。

```cpp
Date(int year = 0, int month = 1, int day = 1)
	//初始化列表
	:_year(year)
	, _month(month)
	,_day(day)
{
	
}
```

**【注意】**

1.每个**成员变量**在初始化列表中**只能出现一次**(初始化只能初始化一次)

2.**类中**包含以下**成员**，**必须**放在初始化列表位置进行**初始化**：

- **引用**成员变量
- **const**成员变量
- **自定义类型**成员(该类**没有默认构造函数**)

```cpp
class B
{
public:
    //可以理解成初始化列表是对象的成员变量定义的地方
    B(int a, int ref)
    :_aobj(a)
    ,_ref(ref)
    ,_n(10)
    {
        //这里也可以赋值，但是提到的那三种类型只能在上面初始化列表赋值
    }
private:
    A _aobj; // 没有默认构造函数
    int& _ref; // 引用
    const int _n; // const
    //成员变量的声明
};
int main()
{
    B b(1 ,2)//对象定义
    return 0;
}
```

**可以理解成初始化列表是对象的成员变量定义的地方**（传参构造对象之后顺便定义）

3.尽量使用初始化列表初始化，因为不管你是否使用初始化列表，对于**自定义类型成员变量**，一定会**先使用初始化列表初始化**。

- **因为自定义类型成员变量会先调用构造函数，构造函数又会定义对象，如果不使用初始化列表，需要先构造一个临时拷贝，再用临时拷贝赋值给自定义变量**

```cpp
class Time
{
public:
	Time(int hour = 0)
		:_hour(hour)
	{
		cout << "Time()" << endl;
	}
private:
	int _hour;
};
class Date
{
public:
	Date(int day)
        //_t是在这里定义的
       :_t(1)//初始化列表
	{
        //如果不使用初始化列表
        //Time t(1);
        //_t = t;
    }
private:
	int _day;
	Time _t;
};
int main()
{
	Date d(1);
}
```

4.**成员变量**在类中**声明次序**就是其在初始化列表中的**初始化顺序**，与其在初始化列表中的**先后次序无关**

- 即：**先声明的则先定义，定义的时候不受先后次序影响**

```cpp
class A
{
public:
	A(int a)
		:_a1(a)
		, _a2(_a1)//所以这里先定义_a2,但此时的_a1还未定义，是个随机值，所以选D
	{}
	void Print() {
		cout << _a1 << " " << _a2 << endl;
	}
private:
	int _a2;//这里先声明了_a2
	int _a1;
}
int main() {
	A aa(1);
	aa.Print();
}
```

> A.输出1 1
> B.程序崩溃
> C.编译不通过
> D.输出1 随机值

### 1.3 explicit关键字	

> `explicit` 关键字用于修饰 **类的构造函数** 或 **类型转换运算符**，其核心作用是 **禁止隐式类型转换**，强制要求通过显式方式调用构造函数或转换操作
>
> ```cpp
> Stock food = Stock("360", 250, 2.5);  // 显式调用构造函数
> Stock food("360", 250, 2.5);         // 隐式调用构造函数
> ```
>
> 

> 构造函数不仅可以构造与初始化对象，**对于单个参数的构造函数，还具有类型转换的作用**
>
> ```cpp
> #include<iostream>
> using namespace std;
> class Date
> {
> public:
> 	Date(int year)
> 		:_year(year)
> 	{}
> 	explicit Date(int year)//如果不想让主函数的函数构造发生隐式类型转换，这里就要加上explicit关键字
> 		:_year(year)
> 	{}
> private:
> 	int _year;
> 	int _month;
> 	int _day;
> };
> 
> int main()
> {
> 	Date d1(2018);
> 	// 用一个整形变量给日期类型对象赋值
> 	// 实际编译器背后会用2019构造一个无名对象(临时变量)，最后用无名对象给d1对象进行赋值,如下图
> 	d1 = 2019;
> }
> ```
>
> ![image-20250304175618051](C:\Users\Aufire\AppData\Roaming\Typora\typora-user-images\image-20250304175618051.png)
>
> 上述代码可读性不是很好，**用explicit修饰构造函数，将会禁止单参构造函数的隐式转换。**

## 2.static成员

### 2.1概念

> 声明为**static的类成员**称为类的**静态成员**，用static修饰的**成员变量**，称之为**静态成员变量**；用static修饰的**成员函数**，
>
> 称之为**静态成员函数**。

**静态的成员变量**一定要在**类外**进行**初始化**

**【面试题】**

> 实现一个类，计算中程序中创建出了多少个类对象。

```cpp
class A
{
public:
	A() 
    { 
        ++_scount; 
    }
	A(const A& t)
    { 
        ++_scount;
    }
	static int GetACount() 
    //将成员函数改为静态成员函数，与普通函数不同的是，这里没有this指针，不用对象去掉，不需要传this指针,
    //所以可以直接这样访问A::GetACount
    { 
        return _scount; 
    }
private:
	static int _scount;//在这里声明，不是属于某个对象，是属于类的所有对象，属于这个类
    //_scount不在对象中，而是在静态区，所以每次_scoutn++都是加的同一个变量，不是每个都独立了
};

int A::_scount = 0;//定义：静态的成员变量一定要在类外进行初始化，属于A类，不用再加static

int main()
{
	cout << A::GetACount() << endl;//0
	A a1, a2;
	A a3(a1);//a1是传引用，不会调用拷贝构造，所以只有a1,a2两个构造函数和a3一个拷贝函数,总共三个
	cout << A::GetACount() << endl;//3
}
```



### 2.2特性

1.**静态成员**为**所有类对象**所**共享**，不属于某个具体的实例

2.**静态成员变量**必须在**类外定义**，定义时**不再添加static关键字**

3.类静态成员即可用类名::静态成员或者对象.静态成员来**访问**

4.静态成员函数**没有**隐藏的**this指针**，不能访问任何非静态成员

5.静态成员和类的普通成员一样，也有**public、protected、private**这3种访问级别，也可以具有**返回值**

【问题】

- 静态成员函数可以调用非静态成员函数吗？，**不可以**

```cpp
void f1()
{
    
}
static void f2()
{
    f1();//this->f1();静态成员函数没有this指针，所以不能调用非静态成员函数
}
```

- 非静态成员函数可以调用类的静态成员函数吗？**可以**

> **静态成员函数**属于**类本身**，而不是类的某个具体对象。**非静态成员函数**（即普通成员函数）是**绑定到类的对象上的**，
>
> 它们可以通过**类名::静态成员函数名()**的方式调用静态成员函数。

```cpp
void f3()
{
	f4();//突破类域+访问限定符就可以访问（突破类域就是指访问类里面的成员）
    	 //类里面是一个整体，都在类域中，类里面不受访问限定符限制
}
static void f4()
{
	
}
```

> **突破类域访问的两种方法：**
>
> - d1.f4() （d1是对象）
> - Date :: f4()

**【总结】**

**非静态**成员函数可以通过**this指针**访问对象的非静态成员

**静态**成员函数不能使用this指针，因为它**不属于任何对象**

## 3.C++11 的成员初始化新玩法。

> C++11支持非静态成员变量在声明时进行初始化赋值，**但是要注意这里不是初始化，这里是给声明的成员变量缺省值。**

```cpp
class B
{
public:
	B(int b = 0)
		:_b(b)
	{}
private:
	int _b;
};
class A
{
public:
	void Print()
	{
		cout << a << endl;
		cout << b._b << endl;
		cout << p << endl;
	}
private:
	// 非静态成员变量，可以在成员声明时给缺省值。
	int a = 10;
	B b = 20;
	int* p = (int*)malloc(4);
    
	static int n;//静态成员变量不能在这里给缺省值
};
int A::n = 10;
int main()
{
	A a;
	a.Print();
	return 0;
}
```



## 4.友元

> 友元分为：**友元函数**和**友元类**
> 友元提供了一种突破封装的方式，有时提供了便利。但是友元会增加耦合度，破坏了封装，所以友元不宜多用。

### 4.1 友元函数

> ```cpp
> #include<iostream>
> using namespace std;
> class Date
> {
> public:
> 	Date(int year, int month, int day)
> 		: _year(year)
> 		, _month(month)
> 		, _day(day)
> 	{}
> 	ostream& operator<<(ostream& _cout)
> 	{
> 		_cout << _year << "-" << _month << "-" << _day;
> 		return _cout;
> 	}
> private:
> 	int _year;
> 	int _month;
> 	int _day;
> };
> int main()
> {
> 	Date d(2017, 12, 24);
> 	d << cout;
> 	return 0;
> }
> ```
>
> 问题：现在我们尝试去重载operator<<，然后发现我们没办法将operator<<重载成成员函数。**因为cout的输出流对象和隐含的this指针在抢占第一个参数的位置。**this指针默认是第一个参数也就是左操作数了。但是实际使用中**cout需要是第一个形参对象**，才能正常使用。所以我们要将operator<<重载成全局函数。但是这样的话，又会导致类外没办法访问成员，那么这里就需要友元来解决。operator>>同理。
>
> ```cpp
> class Date
> {
> 	friend ostream& operator<<(ostream& _cout, const Date& d);//将类外函数变成友元函数
> 	friend istream& operator>>(istream& _cin, Date& d);		  //使得类外的函数可以访问类内的成员变量
> public:
> 	Date(int year = 0, int month = 1, int day = 1)
> 		: _year(year)
> 		, _month(month)
> 		, _day(day)
> 	{}
> private:
> 	int _year;
> 	int _month;
> 	int _day;
> };
> ostream& operator<<(ostream& _cout, const Date& d)//如果要连续输出，返回值就得跟第一个参数一样
> {
> 	_cout << d._year << "-" << d._month << "-" << d._day;
> 	return _cout;
> }
> istream& operator>>(istream& _cin, Date& d)//不用const，否则就不能输入来改变了
> {
> 	_cin >> d._year;//空格或者是换行都可以依次输入
> 	_cin >> d._month;
> 	_cin >> d._day;
> 	return _cin;
> }
> int main()
> {
> 	Date d;
> 	cin >> d;
> 	cout << d << endl;
>  //cout << d1 << d2;连续输出
> 	return 0;
> }
> ```

**【注意】：**cout是**ostream**类型的，cin是**istream**类型的

**【说明】：**

- **友元函数**可访问类的私有和保护成员，但**不是类的成员函数**
- 友元函数**不能用const修饰**（没有this指针，const修饰的是this指针指向的内容）
- **友元函数**可以在类定义的任何地方声明，**不受类访问限定符限制**
- **一个函数可以是多个类的友元函数**
- 友元函数的调用与普通函数的调用和原理相同

### 4.2 友元类

> 友元类的所有成员函数都可以是另一个类的友元函数，都可以访问另一个类中的非公有成员。
>
> - **友元关系是单向的，不具有交换性**。
>
>   比如上述Time类和Date类，在Time类中声明Date类为其友元类，那么可以在Date类中直接访问Time**(A是B的友元，A可以直接访问B的成员)**
>
>   类的私有成员变量，但想在Time类中访问Date类中私有的成员变量则不行。
>
> - **友元关系不能传递**
>
>   如果B是A的友元，C是B的友元，则不能说明C时A的友元。
>
> ```cpp
> class Date; // 前置声明
> class Time
> {
> 	friend class Date; // 声明日期类为时间类的友元类，则在日期类中就直接访问Time类中的私有成员变量
> public:
> 	Time(int hour = 0, int minute = 0, int second = 0)
> 		: _hour(hour)
> 		, _minute(minute)
> 		, _second(second)
> 	{}
> private:
> 	int _hour;
> 	int _minute;
> 	int _second;
> };
> class Date
> {
> public:
> 	Date(int year = 1900, int month = 1, int day = 1)
> 		: _year(year)
> 		, _month(month)
> 		, _day(day)
> 	{}
> 	void SetTimeOfDate(int hour, int minute, int second)
> 	{
> 		// 直接访问时间类私有的成员变量
> 		_t._hour = hour;
> 		_t._minute = minute;
> 		_t._second = second;
> 	}
> private:
> 	int _year;
> 	int _month;
> 	int _day;
> 	Time _t;
> };
> ```

## 5.内部类

> **【概念】**如果一个类定义在另一个类的内部，这个内部类就叫做内部类。注意此时这个内部类是一个独立的
> 类，它不属于外部类，更**不能通过外部类的对象去调用内部类**。外部类对内部类没有任何优越的访问权限。
>
> 【**注意**】**内部类就是外部类的友元类**。注意友元类的定义，**内部类可以**通过外部类的对象参数来**访问外部类中**
> **的所有成员**。但是外部类不是内部类的友元。
> 特性：
>
> 1. 内部类可以定义在外部类的public、protected、private都是可以的。
> 2. 注意**内部类可以直接访问外部类中的static、枚举成员，不需要外部类的对象/类名**。
> 3. **sizeof(外部类)=外部类，和内部类没有任何关系**。
>
> ```cpp
> class A
> {
> private:
> 	static int k;
> 	int h;
> public:
> 	class B
> 	{
> 	public:
> 		void foo(const A& a)
> 		{
> 			cout << k << endl;//OK
> 			cout << a.h << endl;//OK
> 		}
> 	};
> };
> int A::k = 1;
> int main()
> {
> 	A::B b;//要指定B在A类中，然后构造B类的对象
> 	b.foo(A());
> 	return 0;
> }
> ```

## 匿名对象

```cpp
#include<iostream>
using namespace std;
class Date
{
public:
	Date()
	{
		cout << " Date() " << endl;
	}
	int f(int n)
	{
		cout << " f()" << endl;

		return n;
	}
	~Date()
	{
		cout << "~Date()" << endl;
	}
};
int main()
{
	Date d;
	d.f(10);
	/*Date();*/
	//匿名对象
	Date().f(10);//只有我这一行会使用这个创建对象，别人不需要使用,只是单纯的想调用这个函数		


	return 0;
}
```

【**唯一的区别是**】

- 构造的**对象d**的生命周期是在**整个main函数**
- 而**匿名对象**的生命周期就**在它那一行**，出了那一行就会**自动调用析构函数**
