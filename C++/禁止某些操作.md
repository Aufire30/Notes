> 禁止某些操作主要是用到`delete`关键词

# 禁止默认构造函数

```cpp
class example
{
private:
    example(){};
}
```

```cpp
class example
{
	example() = delete;
}
```

# 禁止拷贝构造

```cpp
example(const example&) = delete;
```

# 禁止赋值操作

```cpp
example& operator=(const example&) = delete;
```

