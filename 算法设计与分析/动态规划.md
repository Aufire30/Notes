# 动态规划概述

## 基本原理

> 动态规划算法通常用于求解具有某种**最优性质**的问题。
>
> 在这类问题中，可能会有许多可行解。每一个解都对应一个值，我们希望**找到具有最优值的解**

基本思想是**将待求解问题分解成若干个子问题**，先求解子问题，然后**从这些子问题的解得到原问题的解**



适用于动态规划求解的问题，经分解得到的子问题往往**不是互相独立**的。

若用分治法来解决这类问题，则分解得到的子问题数目太多了，有些子问题被重复计算了很多次



**基本思路：**可以用一个**表(数组**)来记录所有已解的**子问题的答案**。不管子问题以后是否被用到，只要它被计算过，就将其结果填入表中。(具体的动态规划算法多种多样，但他们具有相同的填表格式)

## 特征

- **最优子结构**：当问题的**最优解包含了其子问题的最优解**时，称该问题具有最优子结构的性质。



- **无后效性**：某一阶段的状态一旦确定，就不受这个状态以后决策的影响。即**某状态**以后的过程**不会影响以前的状态，只与当前状态有关**



- **重叠子问题**：在用递归算法自顶向下解问题时，每次产生的子问题并不总是新问题，有些子问题被反复计算多次。动态规划算法正是利用了这种子问题的重叠性质，对**每一个子问题只解一次**，而后将其解保存在一个表格中，在以后尽可能多地利用这些子问题的解。(**该性质并不是必要条件，但是如果没有这条性质，动态规划算法同其他算法相比就不具备优势**)

## 基本步骤

![image-20260430085056734](C:\Users\Aufire\AppData\Roaming\Typora\typora-user-images\image-20260430085056734.png)

# 数字三角形

![image-20260430093928932](C:\Users\Aufire\AppData\Roaming\Typora\typora-user-images\image-20260430093928932.png)

![image-20260430094027079](C:\Users\Aufire\AppData\Roaming\Typora\typora-user-images\image-20260430094027079.png)

# 求最长公共子序列

![image-20260507092026844](C:\Users\Aufire\AppData\Roaming\Typora\typora-user-images\image-20260507092026844.png)



### 求解过程

![image-20260507095003525](C:\Users\Aufire\AppData\Roaming\Typora\typora-user-images\image-20260507095003525.png)

![image-20260507094916172](C:\Users\Aufire\AppData\Roaming\Typora\typora-user-images\image-20260507094916172.png)

![image-20260509145143820](C:\Users\Aufire\AppData\Roaming\Typora\typora-user-images\image-20260509145143820.png)

![image-20260509145325050](C:\Users\Aufire\AppData\Roaming\Typora\typora-user-images\image-20260509145325050.png)

### 代码实现

```C++
#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
using namespace std;

#define MAX 51
int m, n;
string a, b;
int dp[MAX][MAX];
vector<char> subs;

void LCSlength()
{
    int i, j;
    for (i=0; i<=m; i++)
        dp[i][0] = 0;
    for (j=0; j<=n; j++)
        dp[0][j] = 0;
    for (i=1; i<=m; i++)
        for (j=1; j<=n; j++)
        {
            if (a[i-1] == b[j-1])
                dp[i][j] = dp[i-1][j-1] + 1;
            else
                dp[i][j] = max(dp[i][j-1], dp[i-1][j]);
        }
}

void Buildsubs()
{
    int k = dp[m][n];
    int i = m;
    int j = n;
    while (k > 0)
        if (dp[i][j] == dp[i-1][j])
            i--;
        else if (dp[i][j] == dp[i][j-1])
            j--;
        else
        {
            subs.push_back(a[i-1]);
            i--; j--; k--;
        }
    reverse(subs.begin(), subs.end());//最后反转得到的逆序的subs
}

int main()
{
    a = "abcbdb";
    b = "acbbabdbb";
    m = a.size();
    n = b.size();
    LCSlength();
    Buildsubs();
    cout << "LCS长度：" << dp[m][n] << endl;
    cout << "LCS序列：";
    for (char c : subs) cout << c;
    cout << endl;
    return 0;
}
```

